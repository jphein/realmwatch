venv/
.venv/
__pycache__/
*.pyc
node_modules/
*.js.map
*.rrd
dist/
model-cache/
realm-map.monolith.js

# Runtime data
.map_server.pid
realm.db
realm.db-shm
realm.db-wal
monitor_3min.log
topology.json

# realm CLI install-time version stamp (written by `make cli-install`)
scripts/cli/.realm-version

# Screenshots / preview assets (not consumed by the served pages)
report-card-*.png
wifi-guide-*.png

# Secrets
.env
.mcp.json

# Per-realm private inventory + family-facing pages — never committed.
# See scripts/lib/fleet.sh.example for the template you copy locally to
# scripts/lib/fleet.sh with your own infrastructure.
scripts/lib/fleet.sh
wifi-guide.html
report-card.html

# Large files
access-to-power.pdf

# Git worktrees
.worktrees/

# Brainstorm sessions
.superpowers/

# Playwright MCP cache
.playwright-mcp/

# Claude Code session data (keep skills)
.claude/*
!.claude/skills/
!.claude/skills/**

# Private content — JP's specific realm narrative + voice configs
docs/codex/
personas.json

# Realm-local config (JP-specific data: herald templates, node overrides, layout buildings)
realm-local.json

# Fleet catalog — JP-specific MACs, hostnames, VLANs; never tracked
fleet.yaml

# Migration backups (created by scripts/migrate-fleet.py)
realm.db.pre-fleet-*
personas.json.pre-fleet-*
realm-local.json.pre-fleet-*
venv.legacy/
*.egg-info/
